################################################################################
#### FOR NEXT COMMIT DESCRIPTION ###############################################

Refactorization.

Signal handling for SIGINT.

Non-block for the device file.
Random.


ADD Receiving and interpreting note on/off midi messages

This dirty version is able to locate the device file for MIDI
devices (for ALSA driver) though some hints were included to
support older drivers (e.g. OSS). It can open the device
file and listen to the MIDI messages sent by the keyboard.
So far it supports two types of messages: channel/voice messages:
note on (key is pressed) and note off (key is released).
The program is able to parse these two types of messages and
to decode the key id into a latin note naming style.

This is a draft to proof the concept. The next steps will
probably be to clean it up, re-factor it and probably to advance
into a basic trainer so I can begin to use it to practice my
piano.

################################################################################
################################################################################

################################################################################
### TODO's #####################################################################

* A timer thread that can be reset/cancelled/restarted from another
thread (e.g. the midi listener
* The midi messages treatment should be more robust for the
state machine of the interpreter does not remain in an unrecoveranle
state if a message with a number of data bytes different than 2
is received.
* Keep researching the alternative aproaches using the ALSA library
(sequencer, midiraw).

################################################################################
################################################################################

################################################################################
### DONE TODO's ################################################################

* Use O_NONBLOCK for the device file.
* The midi interface for at least note on and off messages.
* Conversions from key number to:
	* Absolute note (do2).
	* Relative note (do).
	* Anglo-saxon vs latin note naming.
* Try the O_ASYNC option for open() to migrate to a multi-threaded
program.
	* Apparently it's not supported by the Alsa driver.
* Catch ^C.


################################################################################
################################################################################




################################################################################
#### OLD COMMIT DESCRIPTIONS ###################################################

No compiling, valgrind, norminette or doxygen unexpected errors
shown.

Documentation updated.

